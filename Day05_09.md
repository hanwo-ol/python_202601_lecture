# 정렬?
일주일간 나오시느라 고생많으셨습니다. 오늘의 추천영상입니다. 알고리즘 시간에 별로 언급이 안된 big O에 대한 개념영상입니다. 
> https://www.youtube.com/watch?v=BEVnxbxBqi8

# 알고리즘 복습
## 정렬 및 탐색 알고리즘 실습
12명의 학생의 각각의 점수가 있다. 
1. 시험 등수가 3등까지 장학금을 받을 수 있을 때, 장학금을 받기 위한 최소 점수는 몇 점인가?
2. 시험 점수가 90점 이상이면 장학금을 받을 수 있을 때, 장학금을 받을 수 있는 학생은 몇 명인가?

[83, 91, 76, 88, 95, 69, 84, 90, 72, 87, 93, 81]


정렬 및 탐색 알고리즘을 이용해서 문제를 풀어라.
단, 컴퓨팅사고를 통해야 하며, 자연어로 작성하고, 수도 코드를 작성한 다음, 파이썬으로 구현한 후, 최종적으로 함수 형태로 기능을 구현하라

### 조교의 뻘짓

## 0. 컴퓨팅 사고(Computational Thinking) 접근

1.  **분해 (Decomposition):** 문제를 두 개의 작은 문제로 나눕니다.
    *   문제 A: 점수를 순서대로 나열하여 3등의 점수 찾기 (정렬 및 인덱싱).
    *   문제 B: 특정 조건(90점 이상)을 만족하는 데이터의 개수 세기 (탐색 및 카운팅).
2.  **패턴 인식 (Pattern Recognition):**
    *   "3등까지"라는 말은 점수가 높은 순서대로 나열(내림차순 정렬)했을 때 3번째 위치한 값을 의미합니다.
    *   "90점 이상"은 리스트의 모든 요소를 순회하며 조건(`>=`)을 검사하는 선형 탐색 패턴입니다.
3.  **추상화 (Abstraction):**
    *   학생의 이름이나 다른 정보는 불필요하며, 오직 '점수 리스트'만 중요합니다.
    *   장학금 커트라인은 정렬된 리스트의 `index 2` (0부터 시작하므로) 값입니다.
4.  **알고리즘 설계 (Algorithm Design):**
    *   데이터 입력 $\rightarrow$ 내림차순 정렬 $\rightarrow$ 3번째 값 추출 $\rightarrow$ 조건에 맞는 개수 탐색 $\rightarrow$ 결과 출력.

---

## 1. 자연어 알고리즘 (Natural Language)

1.  학생 12명의 점수 리스트를 준비합니다.
2.  **[정렬]** 점수가 높은 학생이 먼저 오도록 리스트를 **내림차순**으로 정렬합니다.
3.  **[문제 1 해결]** 정렬된 리스트에서 0번째, 1번째, 2번째 학생까지가 장학금 대상입니다. 따라서 3등에 해당하는 **2번째 인덱스**의 점수를 확인합니다. 이 점수가 장학금을 받기 위한 최소 점수(커트라인)가 됩니다.
4.  **[문제 2 해결]** 리스트에 있는 모든 점수를 하나씩 확인합니다(**탐색**). 만약 점수가 90점 이상이라면 숫자를 1씩 셉니다.
5.  구한 커트라인 점수와 90점 이상인 학생 수를 결과로 반환합니다.

---

## 2. 수도 코드 (Pseudo Code)

```text
START
    // 데이터 입력
    Scores = [83, 91, 76, 88, 95, 69, 84, 90, 72, 87, 93, 81]

    // 1. 정렬 알고리즘 (내림차순)
    SORT Scores in Descending Order

    // 2. 문제 1: 3등까지 장학금 (최소 점수 찾기)
    // 배열 인덱스는 0, 1, 2 이므로 3등은 index 2
    Min_Scholarship_Score = Scores[2]

    // 3. 문제 2: 90점 이상 학생 수 구하기 (선형 탐색)
    High_Score_Count = 0
    FOR each score IN Scores DO
        IF score >= 90 THEN
            High_Score_Count = High_Score_Count + 1
        END IF
    END FOR

    // 결과 출력
    PRINT "장학금 최소 점수:", Min_Scholarship_Score
    PRINT "90점 이상 학생 수:", High_Score_Count
END
```

---

## 3. 파이썬 구현 (Python Code)

```python
# 데이터 정의
scores = [83, 91, 76, 88, 95, 69, 84, 90, 72, 87, 93, 81]

# 1. 정렬 (Sorting): 내림차순
# 파이썬의 리스트 내장 함수 sort()는 Timsort 알고리즘(O(N log N))을 사용합니다.
scores.sort(reverse=True)

# 정렬된 결과 확인 (디버깅용)
# [95, 93, 91, 90, 88, 87, 84, 83, 81, 76, 72, 69]
print(f"정렬된 점수: {scores}")

# 2. 문제 1 해결: 3등 점수 찾기 (인덱싱)
# 1등(idx 0), 2등(idx 1), 3등(idx 2)
scholarship_cutoff = scores[2]

# 3. 문제 2 해결: 90점 이상 찾기 (탐색)
count_over_90 = 0
for score in scores:
    if score >= 90:
        count_over_90 += 1

# 결과 출력
print(f"1. 장학금을 받기 위한 최소 점수: {scholarship_cutoff}점")
print(f"2. 90점 이상 학생 수: {count_over_90}명")
```

---

## 4. 함수 형태 구현 (Final Function)

최종적으로 재사용 가능한 함수 형태로 코드를 구조화합니다.

```python
def analyze_scholarship(score_list, rank_limit, score_limit):
    """
    학생 점수를 분석하여 장학금 커트라인과 우수 학생 수를 반환하는 함수
    
    Args:
        score_list (list): 학생들의 점수 리스트
        rank_limit (int): 장학금을 받는 등수 (예: 3등까지)
        score_limit (int): 우수 학생 기준 점수 (예: 90점 이상)
        
    Returns:
        tuple: (커트라인 점수, 기준 점수 이상 학생 수)
    """
    # 원본 데이터 보존을 위해 복사 후 정렬 (Side Effect 방지)
    sorted_scores = sorted(score_list, reverse=True)
    
    # 1. 장학금 커트라인 구하기 (인덱스는 '등수 - 1')
    # 예외 처리: 학생 수가 등수보다 적을 경우 고려
    if len(sorted_scores) >= rank_limit:
        cutoff_score = sorted_scores[rank_limit - 1]
    else:
        cutoff_score = sorted_scores[-1] # 학생 수가 부족하면 꼴등 점수가 커트라인

    # 2. 기준 점수 이상 학생 수 구하기 (선형 탐색)
    count = 0
    for score in sorted_scores:
        if score >= score_limit:
            count += 1
        else:
            # 내림차순 정렬되어 있으므로, 기준 점수보다 낮아지는 순간
            # 뒤의 데이터는 볼 필요 없이 반복 종료 (효율성 최적화 - Early Exit)
            break
            
    return cutoff_score, count

# --- 실행부 ---
students_scores = [83, 91, 76, 88, 95, 69, 84, 90, 72, 87, 93, 81]

# 함수 호출
cutoff, high_scorers = analyze_scholarship(students_scores, 3, 90)

print(f"=== 결과 확인 ===")
print(f"장학금 수여 가능 최소 점수(3등): {cutoff}점")
print(f"90점 이상 학생 수: {high_scorers}명")
```

### 실행 결과 해석
1.  **정렬 결과:** `[95, 93, 91, 90, 88, 87, 84, 83, 81, 76, 72, 69]`
2.  **문제 1:** 3등(인덱스 2)의 점수는 **91점**입니다.
3.  **문제 2:** 90점 이상인 점수는 `95, 93, 91, 90`으로 총 **4명**입니다.

---

# CSV 데이터 다루기
## 데이터 구조화?
### 테이블?

## CSV 실습

## 1. 데이터 생성 및 CSV 저장하기

먼저 실습에 필요한 성적 데이터를 만들고, 이를 `score.csv`라는 파일로 저장해 보겠습니다. 일반적으로 데이터 분석에는 **Pandas** 라이브러리가 가장 많이 사용됩니다.

```python
# [코드 1] 데이터 생성 및 CSV 저장
import pandas as pd

# 1. 딕셔너리 형태로 데이터 정의
data = {
    '국어': [90, 78],
    '영어': [80, 96],
    '수학': [100, 85]
}

# 2. DataFrame(표) 형태로 변환
df = pd.DataFrame(data)

# 학생 구분을 위해 인덱스 이름 설정 (선택사항)
df.index = ['학생A', '학생B']

# 데이터 확인
print("=== 생성된 데이터 ===")
print(df)

# 3. CSV 파일로 저장 (한글 깨짐 방지를 위해 encoding='utf-8-sig' 사용 권장)
df.to_csv('score.csv', encoding='utf-8-sig')

print("\nSaved 'score.csv' successfully!")
```

---

## 2. CSV 파일 불러오기

방금 저장한 `score.csv` 파일을 파이썬으로 다시 읽어와 보겠습니다. 엑셀 파일을 여는 것과 비슷한 과정입니다.

```python
# [코드 2] CSV 파일 읽기
import pandas as pd

# CSV 파일 불러오기 (첫 번째 열을 인덱스로 사용하려면 index_col=0 옵션 사용)
# 위에서 저장할 때 인덱스도 같이 저장했으므로 불러올 때 처리해줍니다.
loaded_df = pd.read_csv('score.csv', index_col=0)

print("=== 불러온 데이터 확인 ===")
print(loaded_df)
```

---

## 3. 데이터 분석 및 조작 (실습)

불러온 성적표를 활용해 새로운 정보를 만들어 봅시다.
각 학생의 **총점**과 **평균**을 계산하여 새로운 열(Column)에 추가합니다.

```python
# [코드 3] 총점과 평균 계산하기

# 1. 총점 계산 (가로 방향 합계: axis=1)
loaded_df['총점'] = loaded_df.sum(axis=1)

# 2. 평균 계산 (총점 / 3과목)
loaded_df['평균'] = loaded_df['총점'] / 3

# 3. 평균을 소수점 둘째 자리까지만 표현 (round 함수)
loaded_df['평균'] = loaded_df['평균'].round(2)

print("=== 분석 결과 ===")
print(loaded_df)
```

---

## 4. (보너스) CSV 모듈만 사용하기

만약 Pandas 라이브러리를 사용할 수 없는 환경이라면, 파이썬 기본 내장 모듈인 `csv`를 사용할 수도 있습니다. 구조를 이해하는 데 도움이 됩니다.

```python
# [코드 4] 기본 csv 모듈로 파일 읽기
import csv

print("=== 기본 csv 모듈로 읽기 ===")
with open('score.csv', 'r', encoding='utf-8-sig') as f:
    reader = csv.reader(f)
    for row in reader:
        print(row)
```

---

# 📘 [실습] 파이썬 문자열 데이터 전처리 완벽 가이드

데이터 분석 현장에서 만나는 데이터는 깔끔하지 않은 경우가 많습니다. 공백이 들어가 있거나, 대소문자가 섞여 있거나, 불필요한 문자가 붙어있죠.
이번 실습에서는 파이썬의 강력한 문자열 메소드들을 사용하여 데이터를 내 입맛대로 요리하는 방법을 배웁니다.

**학습 목표:**
1. 대소문자 변환 및 문자열 검색
2. 불필요한 공백 제거 및 문자 교체
3. 문자열 자르기(Split)와 합치기(Join)
4. 문자열 구성 확인하기 (숫자/문자 판별)

---

## 1. 대소문자 변환 (Normalization)

데이터를 비교하거나 정렬할 때, 대소문자를 통일하는 것은 기본입니다.

*   `upper()`: 모두 대문자로
*   `lower()`: 모두 소문자로
*   `capitalize()`: 문장의 첫 글자만 대문자로
*   `title()`: 각 단어의 첫 글자를 대문자로

```python
text = "python proGRAMMING is FUN"

print("원본:", text)
print("대문자:", text.upper())
print("소문자:", text.lower())
print("첫 글자만:", text.capitalize())
print("제목 형식:", text.title())
```

---

## 2. 문자열 검색 및 개수 세기

특정 단어가 들어있는지, 몇 번 나오는지 확인합니다.

*   `count()`: 특정 문자의 개수 반환
*   `find()`: 특정 문자의 위치(인덱스) 찾기 (없으면 -1)
*   `index()`: 특정 문자의 위치 찾기 (없으면 오류 발생)
*   `startswith()`: 특정 문자로 시작하는지 확인 (True/False)
*   `endswith()`: 특정 문자로 끝나는지 확인 (True/False)

```python
msg = "Hello Python, Hi Python!"

# 1. 'Python'이라는 단어가 몇 번 나오나?
cnt = msg.count("Python")
print(f"'Python' 등장 횟수: {cnt}회")

# 2. 첫 번째 'Python'은 어디에 있나? (인덱스 번호)
loc = msg.find("Python")
print(f"첫 번째 위치 인덱스: {loc}")

# 3. 특정 단어로 시작/끝나는가? (검증용)
file_name = "report.csv"
print("csv 파일인가요?", file_name.endswith(".csv"))
print("report로 시작하나요?", file_name.startswith("report"))
```

---

## 3. 공백 제거 및 문자열 교체 (Cleaning)

데이터 전처리의 핵심입니다. 양옆의 불필요한 공백을 지우거나 오타를 수정합니다.

*   `strip()`: 양쪽 공백 제거 (가장 많이 씀!)
*   `lstrip()` / `rstrip()`: 왼쪽 / 오른쪽 공백 제거
*   `replace(old, new)`: 문자를 다른 문자로 변경

```python
dirty_text = "   $$$ 중요 데이터 $$$   "

# 1. 양쪽 공백 제거
clean_1 = dirty_text.strip()
print(f"공백 제거 전: '{dirty_text}'")
print(f"공백 제거 후: '{clean_1}'")

# 2. 불필요한 기호($) 제거 (replace 활용)
# 팁: 제거하고 싶은 문자를 빈 문자열("")로 치환하면 삭제 효과가 납니다.
clean_2 = clean_1.replace("$", "")
print(f"기호 제거 후: '{clean_2}'") # 가운데 공백은 남아있음

# 3. 모든 공백을 없애고 싶다면?
clean_3 = clean_1.replace(" ", "").replace("$", "")
print(f"완전 제거 후: '{clean_3}'")
```

---

## 4. 문자열 자르기와 합치기 (Split & Join) 

CSV 데이터를 다룰 때 가장 중요한 기능입니다. 긴 문자열을 리스트로 만들거나, 리스트를 다시 문자열로 만듭니다.

*   `split(기준)`: 기준 문자로 잘라서 **리스트(List)**로 반환
*   `join(리스트)`: 리스트의 요소들을 특정 문자로 연결하여 **문자열(String)**로 반환

```python
data = "국어,영어,수학,과학"

# 1. 쉼표(,)를 기준으로 자르기
subject_list = data.split(",")
print("리스트 변환:", subject_list)
print("세 번째 과목:", subject_list[2])

# 2. 다시 합치기 (구분자를 '-'로 변경)
new_data = "-".join(subject_list)
print("문자열 결합:", new_data)
```

---

## 5. 문자열 구성 확인 (Validation)

입력된 데이터가 숫자인지, 문자인지 검사할 때 사용합니다. (회원가입, 데이터 검증 등)

*   `isdigit()`: 숫자로만 구성되어 있는지?
*   `isalpha()`: 문자로만(한글/영어) 구성되어 있는지?
*   `isalnum()`: 숫자와 문자로만 구성되어 있는지? (특수문자 X)

```python
s1 = "12345"
s2 = "Python"
s3 = "User123"

print(f"'{s1}'은(는) 숫자인가? {s1.isdigit()}")
print(f"'{s2}'은(는) 문자인가? {s2.isalpha()}")
print(f"'{s3}'은(는) 숫자+문자인가? {s3.isalnum()}")
```

---

## 지저분한 데이터 정리하기

실제 현업 데이터와 유사한 상황입니다. 아래 `raw_data` 리스트에는 사람 이름과 점수가 뒤죽박죽 섞여 있습니다. 이를 깔끔한 딕셔너리 형태로 정리해보세요.

**목표:**
1. 앞뒤 공백 제거
2. "점" 글자 제거
3. 이름과 점수 분리
4. 점수는 정수형(int)으로 변환

```python
raw_data = [
    "  김철수 : 90점  ",
    "이영희:85점",
    "  박민수 :  95 점 "
]

clean_data = {}

print("=== 데이터 정제 과정 ===")

for item in raw_data:
    # 1. 양쪽 공백 제거
    temp = item.strip()
    
    # 2. '점' 글자 제거 및 내부 공백 제거 (replace 체이닝)
    temp = temp.replace("점", "").replace(" ", "")
    
    # 3. 콜론(:) 기준으로 이름과 점수 분리
    name, score = temp.split(":")
    
    # 4. 딕셔너리에 저장 (점수는 숫자로 변환)
    clean_data[name] = int(score)
    
    print(f"변환 완료: {name} -> {score}")

print("\n=== 최종 결과 ===")
print(clean_data)
```

---

# 정규 표현식

## 1. 정규 표현식이란?
문자열에서 특정한 형태나 패턴(Pattern)을 찾아내기 위한 **식(Expression)**입니다.
수많은 `if`, `for`, `split`, `replace` 구문을 단 한 줄의 패턴으로 압축할 수 있습니다.

*   **필요성:**
    *   "주민등록번호 뒷자리를 *로 가려줘."
    *   "이 문서에서 이메일 주소만 다 추출해줘."
    *   "전화번호 형식이 맞는지 검사해줘."
    *   위와 같은 요구사항을 일반 코드로 구현하면 수십 줄이 되지만, 정규식을 쓰면 2~3줄로 끝납니다.

---

## 2. 메타 문자 (Meta Characters) : 문법의 기초
정규 표현식에서 특별한 용도로 사용되는 문자들입니다. 이 기호들을 조합하여 패턴을 만듭니다.

### 2.1 문자 클래스 (Character Class)
| 기호 | 설명 | 동등한 표현 |
| :--- | :--- | :--- |
| `.` | 줄바꿈(`\n`)을 제외한 **모든 문자 1개** | |
| `\d` | **숫자** (Digit) | `[0-9]` |
| `\D` | 숫자가 **아닌** 것 | `[^0-9]` |
| `\w` | **문자+숫자** (Word) | `[a-zA-Z0-9_]` |
| `\W` | 문자+숫자가 **아닌** 것 (특수문자, 공백 등) | `[^a-zA-Z0-9_]` |
| `\s` | **공백** (Space, Tab, Newline) | `[ \t\n\r\f\v]` |
| `[]` | 괄호 안의 문자 중 **하나** | `[abc]` : a 또는 b 또는 c |

### 2.2 수량자 (Quantifiers) : 몇 번 반복되는가?
| 기호 | 설명 |
| :--- | :--- |
| `*` | 0번 이상 반복 (없어도 됨) |
| `+` | 1번 이상 반복 (최소 1개 필수) |
| `?` | 0번 또는 1번 (있거나 없거나) |
| `{n}` | 정확히 n번 반복 |
| `{n, m}` | n번 이상, m번 이하 반복 |

### 2.3 위치 지정자 (Anchors)
| 기호 | 설명 |
| :--- | :--- |
| `^` | 문자열의 **시작** |
| `$` | 문자열의 **끝** |

---

## 3. 파이썬 `re` 모듈 사용법
파이썬은 표준 라이브러리 `re`를 통해 정규 표현식을 지원합니다.

> **참고:** 패턴 문자열 앞에는 항상 `r`을 붙이는 것이 좋습니다. (Raw String)
> 예: `r"\d+"` (백슬래시를 이스케이프 문자로 처리하지 않음)

### 주요 함수
1.  **`re.match(pattern, string)`**: 문자열의 **처음부터** 패턴과 일치하는지 확인.
2.  **`re.search(pattern, string)`**: 문자열 **전체**를 훑어서 첫 번째로 일치하는 것을 찾음.
3.  **`re.findall(pattern, string)`**: 일치하는 **모든** 문자열을 리스트로 반환. (가장 많이 사용)
4.  **`re.sub(pattern, replace, string)`**: 패턴과 일치하는 부분을 다른 문자로 **교체**.

---

## 4. 실습 예제 (Code Practice)

### 예제 1: 전화번호 추출하기 (`\d`와 `{}`)
문자열 속에 숨어있는 핸드폰 번호를 모두 찾아봅니다.
*   패턴 분석: `010` - `숫자4개` - `숫자4개`
*   정규식: `010-\d{4}-\d{4}`

```python
import re

text = "문의사항은 010-1234-5678 또는 010-9876-5432로 연락 주세요. 집 전화는 02-123-4567 입니다."

# 패턴 정의
pattern = r"010-\d{4}-\d{4}"

# 모두 찾기
phones = re.findall(pattern, text)

print(phones) 
# 출력: ['010-1234-5678', '010-9876-5432']
# (02-로 시작하는 번호는 패턴과 다르므로 추출되지 않음)
```

### 예제 2: 이메일 유효성 검사 (`[]`, `+`, `@`)
사용자가 입력한 이메일이 올바른 형식인지 검사합니다.
*   패턴 분석: `문자숫자들` @ `문자숫자들` . `문자들`
*   정규식: `^[\w]+@[\w]+\.[a-zA-Z]+$`

```python
import re

def validate_email(email):
    # ^ : 시작, $ : 끝 (전체가 일치해야 함)
    # [\w]+ : 문자나 숫자가 1개 이상
    # \. : 실제 점(.) 문자 (역슬래시 필요)
    pattern = r"^[\w]+@[\w]+\.[a-zA-Z]+$"
    
    if re.match(pattern, email):
        return True
    else:
        return False

print(validate_email("test@gmail.com"))  # True
print(validate_email("test@gmail"))      # False (.com 없음)
print(validate_email("testgmail.com"))   # False (@ 없음)
```

### 예제 3: 개인정보 마스킹 (`sub`와 그룹 `()`)
주민등록번호 뒷자리를 `*******`로 변경합니다.
*   패턴 분석: `(생년월일6자리)` - `(뒷자리7자리)`
*   정규식: `(\d{6})-(\d{7})`

```python
import re

data = "홍길동의 주민번호는 900101-1234567 입니다."

# sub(패턴, 바꿀문자열, 대상문자열)
# \g<1>은 첫 번째 그룹(앞 6자리)을 그대로 쓰겠다는 의미
masked_data = re.sub(r"(\d{6})-(\d{7})", r"\g<1>-*******", data)

print(masked_data)
# 출력: 홍길동의 주민번호는 900101-******* 입니다.
```

---

## 5. 연습 문제 (Exercise)

다음 문제를 정규 표현식을 사용하여 해결해 보세요.

**문제 1.**
다음 문장에서 'http'나 'https'로 시작하는 웹사이트 주소만 추출하시오.
```python
text = "구글 주소는 https://google.com 이고, 네이버는 http://naver.com 입니다."
```

**문제 2.**
다음 리스트에서 '김'씨 성을 가진 사람의 이름만 추출하시오. (한글 범위: `[가-힣]`)
```python
names = ["김철수", "이영희", "박민수", "김영희", "최수종"]
# 힌트: ^김 으로 시작하고 뒤에 한글이 2글자 더 옴
```

---

## 연습 문제 정답 코드

```python
import re

# 문제 1 정답
text1 = "구글 주소는 https://google.com 이고, 네이버는 http://naver.com 입니다."
# https? -> s가 있거나 없거나 (http 또는 https)
# :// -> 구분자
# \S+ -> 공백이 아닌 문자가 계속됨
urls = re.findall(r"https?://\S+", text1)
print(f"문제 1 정답: {urls}")


# 문제 2 정답
name_list = ["김철수", "이영희", "박민수", "김영희", "최수종"]
kim_names = []

for name in name_list:
    # ^김 : 김으로 시작
    # [가-힣]{2} : 한글이 뒤에 2글자 더 있음 (총 3글자 이름 가정)
    if re.match(r"^김[가-힣]+", name):
        kim_names.append(name)

print(f"문제 2 정답: {kim_names}")
```
